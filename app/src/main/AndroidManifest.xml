<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Foreground service permissions for long-running data transfers (API 34+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Media read permissions for photos/videos (API 33+) and partial access (API 34+) -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- Allow user-selected partial media access on Android 14+ -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED"
        tools:targetApi="34" />
    <!-- Legacy external storage read for Android 12L/12 (maxSdk 32) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <application
        android:name="com.lurenjia534.skydrivex.SkyDriveXApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkyDriveX">
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SkyDriveX"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|layoutDirection|uiMode|screenLayout|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.OobeActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyDriveX" />

        <activity android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="msauth"
                    android:host="com.lurenjia534.skydrivex"
                    android:path="/rZDXYaNZmghPivXu+4dDWNfayVo=" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyDriveX" />

        <activity
            android:name=".ui.activity.PhotoSyncActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyDriveX" />

        <activity
            android:name=".ui.activity.AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyDriveX" />

        <activity
            android:name=".ui.activity.preview.ImagePreviewActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyDriveX" />

        <activity
            android:name=".ui.activity.preview.VideoPreviewActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyDriveX" />

        <activity
            android:name=".ui.activity.preview.AudioPreviewActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyDriveX" />

        <receiver
            android:name=".ui.notification.CancelDownloadReceiver"
            android:exported="false" />

        <!-- Foreground service for user-initiated uploads/downloads -->
        <service
            android:name=".ui.service.TransferService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- Remove default WorkManager initializer so Hilt provided configuration is used -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
